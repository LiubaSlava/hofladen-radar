/**
 * Hofladen Radar v2 — Manrope (UI + display) + JetBrains Mono (meta/data).
 * @see globals.css `.hr-*` component tokens
 */
export const typeDisplay = "font-sans font-extrabold tracking-tight text-balance"
export const typeMeta = "font-mono font-normal tabular-nums tracking-wide"
/** Digits, lang switcher codes — Silkscreen pixel face */
export const typePixel = "font-pixel tabular-nums"
/** Brand wordmark next to logo */
export const typeBrand = "font-pixel leading-tight tracking-wide text-primary"
export const typeKicker =
  "font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-primary"

/** Glass card shell (24px radius, soft shadow) — matches landing v2 `.card` */
export const surfaceCapsule =
  "hr-surface overflow-hidden shadow-[var(--shadow-soft)] ring-1 ring-primary/[0.06] backdrop-blur-xl"
export const surfaceCapsulePad = `${surfaceCapsule} p-4`
